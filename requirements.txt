# Optional — only needed for CorridorKeyLoadEXRSequence
# Install one of the two options below:

# Option A — official OpenEXR bindings (recommended)
openexr
imath

# Option B — imageio with FreeImage plugin
# imageio[freeimage]

# Required for aiohttp server routes (YAK 3D Viewport)
aiohttp

# Required for World Labs API nodes
requests

# Optional — only needed for MatAnyone nodes
# Install MatAnyone from: https://github.com/pq-yang/MatAnyone
# cd MatAnyone && pip install -e .
# Or install dependencies individually:
# numpy>=1.21
# opencv-python>=4.8
# scipy>=1.7
# einops>=0.6
# huggingface_hub
# safetensors
