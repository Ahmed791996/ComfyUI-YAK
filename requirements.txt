# Optional — only needed for CorridorKeyLoadEXRSequence
# Install one of the two options below:

# Option A — official OpenEXR bindings (recommended)
openexr
imath

# Option B — imageio with FreeImage plugin
# imageio[freeimage]

# CorridorKey green screen keying
corridorkey @ git+https://github.com/nikopueringer/CorridorKey.git
timm
typer
rich
kornia

# Required for aiohttp server routes (YAK 3D Viewport)
aiohttp

# Required for World Labs API nodes
requests

# Auto background removal (used by MatAnyone auto-mask)
transformers

# ml-sharp: single-image to 3D Gaussian Splatting (Apple)
sharp @ git+https://github.com/apple/ml-sharp.git

# MatAnyone video matting (inference-only deps, skips GUI/C-extension bloat)
matanyone @ git+https://github.com/pq-yang/MatAnyone.git
thinplate @ git+https://github.com/cheind/py-thin-plate-spline.git
omegaconf
easydict
hickle
easing_functions
imageio==2.25.0
imageio[ffmpeg]
